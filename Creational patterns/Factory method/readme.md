## 🏭 Паттерн Factory Method на PHP  
_Фабричный метод_

![](https://refactoring.guru/images/patterns/content/factory-method/factory-method.png)

## 🧠 Описание
**Factory Method** — порождающий паттерн, который определяет интерфейс для создания объектов, но перекладывает решение о конкретном классе на подклассы. Это позволяет расширять систему без изменения существующего кода.

## ⚡ В двух словах
Создание объектов — через метод-фабрику, который можно переопределять.

## 🎯 Когда использовать
- Нужно добавлять новые типы объектов без изменения клиентского кода.
- Конструктор разрастается логикой выбора конкретного класса.
- Есть иерархия «создателей», каждый из которых создает свой продукт.

## 🧪 Пример использования в PHP
### 1) Интерфейс продукта
```php
<?php

interface Product
{
    public function use(): string;
}
```

### 2) Конкретные продукты
```php
<?php

class Car implements Product
{
    public function use(): string
    {
        return 'Drive a car';
    }
}

class Bike implements Product
{
    public function use(): string
    {
        return 'Ride a bike';
    }
}
```

### 3) Создатель и фабричный метод
```php
<?php

abstract class VehicleFactory
{
    abstract public function make(): Product;

    public function deliver(): string
    {
        return $this->make()->use();
    }
}

class CarFactory extends VehicleFactory
{
    public function make(): Product
    {
        return new Car();
    }
}

class BikeFactory extends VehicleFactory
{
    public function make(): Product
    {
        return new Bike();
    }
}
```

### 4) Использование
```php
<?php

$carFactory = new CarFactory();
echo $carFactory->deliver(); // Drive a car

$bikeFactory = new BikeFactory();
echo $bikeFactory->deliver(); // Ride a bike
```

## ✅ Преимущества
- Упрощает расширение системы новыми продуктами.
- Убирает жёсткую привязку к конкретным классам.
- Поддерживает принцип открытости/закрытости.

## ⚠️ Недостатки
- Увеличивает количество классов.
- Может усложнить структуру при большом числе продуктов.
